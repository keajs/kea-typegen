watch_file .flox/env/manifest.toml
watch_file .flox/env/manifest.lock

if has flox; then
  export FLOX_DISABLE_METRICS=true
  direnv_load flox activate --dir "$PWD" -- direnv dump
else
  log_status "flox not found; skipping .flox environment activation"
fi

layout node
